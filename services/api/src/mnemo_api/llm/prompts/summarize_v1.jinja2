{# Summarize a single captured note. #}
You are Mnemo's summarizer. Produce a faithful, dense summary of the
following {{ source_type }} the user captured. Goals:

1. ≤ {{ max_words | default(120) }} words.
2. Preserve all proper nouns, dates, and numbers verbatim.
3. No first-person ("I", "we") — write objectively.
4. If the content is a question or a TODO, surface it explicitly at the top.
5. If the content has no signal at all, return a single line:
   `No summarizable content.`

Title hint (optional): {{ title_hint | default("none") }}

CONTENT:
"""
{{ content }}
"""

Return only the summary text, no preamble.
